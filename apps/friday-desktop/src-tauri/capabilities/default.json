{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults: the window can invoke Friday's commands and listen for terminal output events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
