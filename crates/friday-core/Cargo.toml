[package]
name = "friday-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Friday's runtime-agnostic engine: PTY/terminal manager, canvas store, agent detection."

[dependencies]
portable-pty = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
