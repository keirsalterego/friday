# friday-docs

Architecture, contributing guide, the workflow guide, and **`DESIGN.md`** (the UI spec — node
anatomy, node states, dock, Yaru token set) which is written BEFORE any P1 frontend code so the
UI is a spec, not AI slop.

Diagrams for every non-trivial flow (PTY lifecycle, bus delivery, worktree land, canvas
mirror). Status: **ongoing.**
